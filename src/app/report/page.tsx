import { ReportView } from "@/components/gui/ReportView";

export const metadata = {
  title: "Test Report — Anderson Vanegas",
  description: "pytest-html styled resume report generated from resume.ts and tests.ts",
};

export default function ReportPage() {
  return <ReportView />;
}
