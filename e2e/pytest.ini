[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers -ra
markers =
    smoke: quick checks on every PR
    sanity: targeted integration checks
    regression: full terminal command coverage
    mobile: mobile viewport and sheet behavior
    a11y: accessibility scans with axe
base_url = http://localhost:3000
